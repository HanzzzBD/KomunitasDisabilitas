// modules/users — tipe bersama antar repository modul ini.

/** Keadaan satu pencacah berjendela: nilai sekarang + sisa umur jendela. */
export interface CounterState {
  value: number;
  resetInSeconds: number;
}
