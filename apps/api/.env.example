# Salin file ini menjadi apps/api/.env (JANGAN commit — .gitignore menutup).
#   cp apps/api/.env.example apps/api/.env
#
# PENTING: Prisma CLI & proses API membaca .env dari FOLDER INI (apps/api),
# BUKAN dari root repo. .env di root tidak pernah terbaca oleh prisma migrate.
# Konvensi: .env.example hidup PER APP (di folder app yang membacanya) —
# tidak ada .env.example di root. Variabel app lain: apps/<app>/.env.example.

NODE_ENV=development
HOST=0.0.0.0
PORT=3000
LOG_LEVEL=info

# Compose dev: Postgres di host port 5433 (bukan 5432 — hindari bentrok
# Postgres lokal spt Laragon); user/pass/db = incasif/incasif/incasif.
DATABASE_URL=postgresql://incasif:incasif@localhost:5433/incasif
REDIS_URL=redis://localhost:6379
REDIS_QUEUE_URL=redis://localhost:6380

CORS_ORIGINS=http://localhost:5173
RATE_LIMIT_MAX=300
RATE_LIMIT_WINDOW_MS=60000
