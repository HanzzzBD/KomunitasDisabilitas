{
  "name": "@incasif/api",
  "version": "0.0.0",
  "private": true,
  "description": "Express backend (monolith modular). Placeholder — kode aplikasi menyusul di PR berikutnya.",
  "type": "module",
  "scripts": {
    "lint": "eslint src --ext .ts,.tsx --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@incasif/config": "workspace:*",
    "@types/node": "22.10.2",
    "eslint": "8.57.1",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
